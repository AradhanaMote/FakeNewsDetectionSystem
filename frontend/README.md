# Frontend (React + Vite + Tailwind)

Env:
- `VITE_API_BASE` (e.g., `http://localhost:8000`) for SSE `/stream`

Run:
```bash
cd frontend
npm install
npm run dev
```

Build:
```bash
npm run build && npm run preview
```
